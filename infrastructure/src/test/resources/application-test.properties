# Test profile: H2 in-memory database for @WebMvcTest / @DataJpaTest slice tests.
# Spring Boot auto-configures H2 when the driver is on the classpath (test scope).
# ddl-auto=create-drop is safe for tests — DB is recreated for each test class.

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false
