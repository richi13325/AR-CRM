package com.ar.crm2.model.enums;

/**
 * Semantic state for a board column belonging to a deal board.
 * Used exclusively when {@link TipoTablero} is {@code TRATOS}.
 */
public enum TipoEstadoColumnaTableroTrato {
    ABIERTO,
    GANADO,
    PERDIDO
}