package com.ar.crm2.model.enums;

/**
 * Tipo de entrada en el timeline de un Trato.
 * NOTA: nota manual escrita por un usuario.
 * EVENTO: registro automático del sistema (ej. ganado/perdido).
 */
public enum TipoNota {
    NOTA,
    EVENTO
}
